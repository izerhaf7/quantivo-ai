# Processing + Skeleton

Purpose: reassure user while AI analysis runs.

Rules:
- Show topic being analyzed.
- Show progress/steps.
- Show skeleton report layout with `aria-busy="true"`.
- Avoid blank loading.

Do not confuse this with splash loading. Splash uses logo only; processing uses skeleton.
